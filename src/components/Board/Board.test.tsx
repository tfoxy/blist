import React from 'react';
import { create } from 'react-test-renderer';
import Board from './Board';

describe('Board component', () => {
  /*
  it('should be created successfully', () => {
    const component = create(<Board />);
    const instance = component.root;
    assert(instance);
  });
  */
});
